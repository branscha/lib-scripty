exit the application.
The exact behaviour can vary, you dont want every embedded pane to be able to exit the application immediately.
Therefore the command is an optional one, the embedder has to decide if the user could do this.